interface ErrorProps {
  id: number;
  focus: boolean;
  value: string;
  validValue: boolean;
}

const ErrorInfo = ({ id, focus, value, validValue }: ErrorProps) => {
  return <p></p>;
};

export default ErrorInfo;
